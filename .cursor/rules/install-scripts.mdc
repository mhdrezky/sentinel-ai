---
description: Windows installer and PowerShell script conventions
globs: scripts/**/*.ps1
alwaysApply: false
---

# install.ps1

## Requirements

- `#Requires -Version 5.1`, `$ErrorActionPreference = "Stop"`
- Idempotent; no admin; user-level `uv tool install`
- Check `$LASTEXITCODE` after every `uv` / `sentinel-ai` call

## One-liner (latest release)

```powershell
irm https://github.com/mhdrezky/sentinel-ai/releases/latest/download/install.ps1 | iex
```

Do **not** use raw branch URLs for the public installer.

## Install source

Resolve tag from `GET /repos/mhdrezky/sentinel-ai/releases/latest`, then:

```powershell
uv tool install --force "git+https://github.com/mhdrezky/sentinel-ai.git@$tag"
```

Local dev override: `uv tool install --force --from $resolvedPath sentinel-ai`

## Remote piping (`irm | iex`)

Parameters do not pass through pipe. Support:

```powershell
$env:SENTINEL_REPO_PATH = "D:\path\to\project"
irm …/releases/latest/download/install.ps1 | iex
```

## Host config

Fetch `sentinel.toml` from the **same release tag** as the package install.
Fallback heredoc must stay in sync with `src/sentinel_ai/sentinel.toml`.
Never overwrite existing host config.
